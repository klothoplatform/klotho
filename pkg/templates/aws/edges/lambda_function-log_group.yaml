source: aws:lambda_function
target: aws:log_group
operational_rules:
  - if: '{{ eq (fieldValue "LogGroup" .Source) .Target }}'
    then: 'alert'
    configuration_rules:
      - resource: '{{ .Target }}'
        configuration:
          field: LogGroupName
          value: /aws/lambda/{{ .Source.Name }}
