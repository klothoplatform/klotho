source: aws:lambda_function
target: aws:secret
operational_rules:
  - steps:
      - resource: '{{ .Target }}'
        direction: upstream
        resources:
          - '{{ fieldValue "ExecutionRole" .Source}}'
