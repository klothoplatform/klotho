source: aws:lambda_function
target: aws:dynamodb_table
operational_rules:
  - steps:
      - resource: '{{ .Target }}'
        direction: upstream
        resources:
          - '{{ fieldValue "ExecutionRole" .Source}}'
