// coretesting contains utilities to make testing things involving the `core` types and functions easier.
// This is a separate package from core so it can be depended on only by tests and therefore pruned in non-test builds.
package coretesting
