provider: aws
type: iam_oidc_provider
rules:
  downstream:
  - enforcement: exactly_one
    resource_types: 
    - eks_cluster
    set_field: Cluster
    unsatisfied_action:
      operation: create
delete_context:
  requires_no_upstream: true