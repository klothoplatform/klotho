source: aws:ecs_service
target: aws:secret
operational_rules:
  - steps:
      - resource: '{{ .Target }}'
        direction: upstream
        resources:
          - '{{ fieldValue "ExecutionRole" (fieldValue "TaskDefinition" .Source)}}'
