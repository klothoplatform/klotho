qualified_type_name: kubernetes:service_export
display_name: Service Export

properties:
  Cluster:
    type: resource
    operational_rule:
      steps:
        - direction: downstream
          resources:
            - classifications:
                - cluster
                - kubernetes
  Object:
    type: map
    properties:
      kubernetes:TypeMeta:
        type: model
      Metadata:
        type: model(kubernetes:ObjectMeta)

delete_context:
  requires_no_upstream_or_downstream: true
views:
  dataflow: small
