source: aws:vpc_link
destination: aws:load_balancer

deployment_order_reversed: false
deletion_dependent: false
reuse:
configuration:
  - resource: aws:vpc_link
    config:
      field: Target
      value: aws:load_balancer
