source: aws:efs_access_point
target: aws:efs_mount_target
deployment_order_reversed: true # For resources (lambda) that depend on access points, we need to ensure the mount targets are also deployed before them