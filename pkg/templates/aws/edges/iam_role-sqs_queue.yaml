source: aws:iam_role
target: aws:sqs_queue
operational_rules:
  - configuration_rules:
      - resource: '{{ .Source }}'
        configuration:
          field: InlinePolicies
          value:
            - Name: '{{ .Destination.Name }}-policy'
              Policy:
                Version: '2012-10-17'
                Statement:
                  - Action:
                      - sqs:*
                    Effect: Allow
                    Resource:
                      - '{{ .Destination  }}#Arn'
