source: kubernetes:persistent_volume
target: kubernetes:storage_class
operational_rules:
  - configuration_rules:
      - resource: '{{ .Source }}'
        configuration:
          field: Object.Spec.StorageClassName
          value: '{{ .Source.Object.Metadata.Name }}'
