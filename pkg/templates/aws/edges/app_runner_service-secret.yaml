source: aws:app_runner_service
target: aws:secret
operational_rules:
  - steps:
      - resource: '{{ .Target }}'
        direction: upstream
        resources:
          - '{{ fieldValue "InstanceRole" .Source}}'
