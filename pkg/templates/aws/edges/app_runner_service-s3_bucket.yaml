source: aws:app_runner_service
target: aws:s3_bucket
operational_rules:
  - steps:
      - resource: '{{ .Target }}'
        direction: upstream
        resources:
          - '{{ fieldValue "InstanceRole" .Source}}'
